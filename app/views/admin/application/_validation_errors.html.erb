<% if object.errors.any? %>
  <div id="validation_error">
    <h2><%= pluralize(object.errors.count, "error") %> prohibited a successful saved:</h2>
 
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>